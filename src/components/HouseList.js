import HouseDetail from "./HouseDetail";

const HouseList = (props) => {
    return (
        <React.Fragment>
            <HouseDetail />
        </React.Fragment>
    );
}

export default HouseList;